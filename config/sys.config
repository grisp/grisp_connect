[
    {grisp_cryptoauth, [
        {tls_server_trusted_certs_cb, {certifi, cacerts, []}}
    ]},

    {grisp_connect, [
        {device_linking_token, <<"...">>}
    ]},

    {kernel, [{logger_level, notice}]}
].
